# UpdateAsset2

Update a TRC-10 token's description, URL, and bandwidth limit (issuer only).

- Service: `Wallet` only

```protobuf
rpc UpdateAsset2 (UpdateAssetContract) returns (TransactionExtention) {}
```

See the corresponding HTTP endpoint at [/wallet/updateasset](../../http/asset/updateasset.md).
