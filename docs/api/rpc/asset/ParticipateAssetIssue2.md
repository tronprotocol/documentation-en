# ParticipateAssetIssue2

Participate in a TRC-10 sale (purchase with TRX).

- Service: `Wallet` only

```protobuf
rpc ParticipateAssetIssue2 (ParticipateAssetIssueContract) returns (TransactionExtention) {}
```

See the corresponding HTTP endpoint at [/wallet/participateassetissue](../../http/asset/participateassetissue.md).
