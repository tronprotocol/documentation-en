# CreateAssetIssue2

Build a transaction that issues a TRC-10 token.

- Service: `Wallet` only

```protobuf
rpc CreateAssetIssue2 (AssetIssueContract) returns (TransactionExtention) {}
```

See the corresponding HTTP endpoint at [/wallet/createassetissue](../../http/asset/createassetissue.md).
