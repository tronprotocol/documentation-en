# GetAccount

Look up an account by address.

- Service: both `Wallet` and `WalletSolidity`

```protobuf
rpc GetAccount (Account) returns (Account) {}
```

See the corresponding HTTP endpoint at [/wallet/getaccount](../../http/account/getaccount.md).
