# AccountPermissionUpdate

Update an account's owner / witness / active permissions (multi-sig configuration).

- Service: `Wallet` only

```protobuf
rpc AccountPermissionUpdate (AccountPermissionUpdateContract) returns (TransactionExtention) {}
```

See the corresponding HTTP endpoint at [/wallet/accountpermissionupdate](../../http/account/accountpermissionupdate.md).
