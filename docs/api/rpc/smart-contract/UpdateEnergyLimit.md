# UpdateEnergyLimit

Update a contract's `origin_energy_limit` (deployer only).

- Service: `Wallet` only

```protobuf
rpc UpdateEnergyLimit (UpdateEnergyLimitContract) returns (TransactionExtention) {}
```

See the corresponding HTTP endpoint at [/wallet/updateenergylimit](../../http/smart-contract/updateenergylimit.md).
