# ClearContractABI

Clear a contract's ABI (deployer only).

- Service: `Wallet` only

```protobuf
rpc ClearContractABI (ClearABIContract) returns (TransactionExtention) {}
```

See the corresponding HTTP endpoint at [/wallet/clearabi](../../http/smart-contract/clearabi.md).
