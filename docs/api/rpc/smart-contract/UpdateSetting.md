# UpdateSetting

Update a contract's `consume_user_resource_percent` (deployer only).

- Service: `Wallet` only

```protobuf
rpc UpdateSetting (UpdateSettingContract) returns (TransactionExtention) {}
```

See the corresponding HTTP endpoint at [/wallet/updatesetting](../../http/smart-contract/updatesetting.md).
