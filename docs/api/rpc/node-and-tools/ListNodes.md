# ListNodes

List the peer nodes discovered by this node.

- Service: `Wallet` only

```protobuf
rpc ListNodes (EmptyMessage) returns (NodeList) {}
```

See the corresponding HTTP endpoint at [/wallet/listnodes](../../http/node-and-tools/listnodes.md).
