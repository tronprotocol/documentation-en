# GetBandwidthPrices

Get the historical bandwidth unit price.

- Service: both `Wallet` and `WalletSolidity`

```protobuf
rpc GetBandwidthPrices (EmptyMessage) returns (PricesResponseMessage) {}
```

See the corresponding HTTP endpoint at [/wallet/getbandwidthprices](../../http/node-and-tools/getbandwidthprices.md).
