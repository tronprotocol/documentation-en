# GetBlockByNum2

Look up a block by block number.

- Service: both `Wallet` and `WalletSolidity`

```protobuf
rpc GetBlockByNum2 (NumberMessage) returns (BlockExtention) {}
```

See the corresponding HTTP endpoint at [/wallet/getblockbynum](../../http/block-and-tx-query/getblockbynum.md).
