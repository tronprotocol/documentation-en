# GetNowBlock2

Return the latest block.

- Service: both `Wallet` and `WalletSolidity`

```protobuf
rpc GetNowBlock2 (EmptyMessage) returns (BlockExtention) {}
```

See the corresponding HTTP endpoint at [/wallet/getnowblock](../../http/block-and-tx-query/getnowblock.md).
