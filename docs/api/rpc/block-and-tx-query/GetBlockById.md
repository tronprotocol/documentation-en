# GetBlockById

Look up a block by block hash.

- Service: `Wallet` only

```protobuf
rpc GetBlockById (BytesMessage) returns (Block) {}
```

See the corresponding HTTP endpoint at [/wallet/getblockbyid](../../http/block-and-tx-query/getblockbyid.md).
