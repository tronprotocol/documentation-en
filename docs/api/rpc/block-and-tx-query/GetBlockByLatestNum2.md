# GetBlockByLatestNum2

Get the most recent N blocks.

- Service: `Wallet` only

```protobuf
rpc GetBlockByLatestNum2 (NumberMessage) returns (BlockListExtention) {}
```

See the corresponding HTTP endpoint at [/wallet/getblockbylatestnum](../../http/block-and-tx-query/getblockbylatestnum.md).
