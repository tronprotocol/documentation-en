# GetProposalById

Look up a proposal by ID.

- Service: `Wallet` only

```protobuf
rpc GetProposalById (BytesMessage) returns (Proposal) {}
```

See the corresponding HTTP endpoint at [/wallet/getproposalbyid](../../http/witness-and-governance/getproposalbyid.md).
