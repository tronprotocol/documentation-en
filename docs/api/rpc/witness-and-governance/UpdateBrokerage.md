# UpdateBrokerage

An SR updates its reward-share ratio (brokerage).

- Service: `Wallet` only

```protobuf
rpc UpdateBrokerage (UpdateBrokerageContract) returns (TransactionExtention) {}
```

See the corresponding HTTP endpoint at [/wallet/updateBrokerage](../../http/witness-and-governance/updateBrokerage.md).
