# ListProposals

List all proposals.

- Service: `Wallet` only

```protobuf
rpc ListProposals (EmptyMessage) returns (ProposalList) {}
```

See the corresponding HTTP endpoint at [/wallet/listproposals](../../http/witness-and-governance/listproposals.md).
