# GetChainParameters

Get the current values of all chain parameters.

- Service: `Wallet` only

```protobuf
rpc GetChainParameters (EmptyMessage) returns (ChainParameters) {}
```

See the corresponding HTTP endpoint at [/wallet/getchainparameters](../../http/witness-and-governance/getchainparameters.md).
