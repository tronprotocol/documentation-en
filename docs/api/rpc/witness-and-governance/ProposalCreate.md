# ProposalCreate

Create a chain-parameter proposal (SRs only).

- Service: `Wallet` only

```protobuf
rpc ProposalCreate (ProposalCreateContract) returns (TransactionExtention) {}
```

See the corresponding HTTP endpoint at [/wallet/proposalcreate](../../http/witness-and-governance/proposalcreate.md).
