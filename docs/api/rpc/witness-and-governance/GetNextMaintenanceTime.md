# GetNextMaintenanceTime

Return the start time of the next SR maintenance period.

- Service: `Wallet` only

```protobuf
rpc GetNextMaintenanceTime (EmptyMessage) returns (NumberMessage) {}
```

See the corresponding HTTP endpoint at [/wallet/getnextmaintenancetime](../../http/witness-and-governance/getnextmaintenancetime.md).
