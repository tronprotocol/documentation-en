# ProposalApprove

An SR votes on a proposal (approve / withdraw approval).

- Service: `Wallet` only

```protobuf
rpc ProposalApprove (ProposalApproveContract) returns (TransactionExtention) {}
```

See the corresponding HTTP endpoint at [/wallet/proposalapprove](../../http/witness-and-governance/proposalapprove.md).
