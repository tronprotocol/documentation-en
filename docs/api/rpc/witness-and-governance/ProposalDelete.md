# ProposalDelete

Cancel a proposal you created (proposer only).

- Service: `Wallet` only

```protobuf
rpc ProposalDelete (ProposalDeleteContract) returns (TransactionExtention) {}
```

See the corresponding HTTP endpoint at [/wallet/proposaldelete](../../http/witness-and-governance/proposaldelete.md).
