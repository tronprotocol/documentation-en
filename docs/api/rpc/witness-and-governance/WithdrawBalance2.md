# WithdrawBalance2

Withdraw SR block-production rewards or voter staking rewards to balance.

- Service: `Wallet` only

```protobuf
rpc WithdrawBalance2 (WithdrawBalanceContract) returns (TransactionExtention) {}
```

See the corresponding HTTP endpoint at [/wallet/withdrawbalance](../../http/witness-and-governance/withdrawbalance.md).
