# UpdateWitness2

Update an SR candidate's URL (the candidate themselves only).

- Service: `Wallet` only

```protobuf
rpc UpdateWitness2 (WitnessUpdateContract) returns (TransactionExtention) {}
```

See the corresponding HTTP endpoint at [/wallet/updatewitness](../../http/witness-and-governance/updatewitness.md).
