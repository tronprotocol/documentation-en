# ListWitnesses

List all SR candidates.

- Service: both `Wallet` and `WalletSolidity`

```protobuf
rpc ListWitnesses (EmptyMessage) returns (WitnessList) {}
```

See the corresponding HTTP endpoint at [/wallet/listwitnesses](../../http/witness-and-governance/listwitnesses.md).
