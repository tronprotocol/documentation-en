# UnDelegateResource

Undelegate resources (Stake 2.0) and reclaim the delegated amount.

- Service: `Wallet` only

```protobuf
rpc UnDelegateResource (UnDelegateResourceContract) returns (TransactionExtention) {}
```

See the corresponding HTTP endpoint at [/wallet/undelegateresource](../../http/stake-v2/undelegateresource.md).
