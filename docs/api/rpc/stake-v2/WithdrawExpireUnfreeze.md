# WithdrawExpireUnfreeze

Withdraw the matured unfreeze TRX into the balance (Stake 2.0).

- Service: `Wallet` only

```protobuf
rpc WithdrawExpireUnfreeze (WithdrawExpireUnfreezeContract) returns (TransactionExtention) {}
```

See the corresponding HTTP endpoint at [/wallet/withdrawexpireunfreeze](../../http/stake-v2/withdrawexpireunfreeze.md).
