# DelegateResource

Delegate already-frozen resources to another account (Stake 2.0).

- Service: `Wallet` only

```protobuf
rpc DelegateResource (DelegateResourceContract) returns (TransactionExtention) {}
```

See the corresponding HTTP endpoint at [/wallet/delegateresource](../../http/stake-v2/delegateresource.md).
