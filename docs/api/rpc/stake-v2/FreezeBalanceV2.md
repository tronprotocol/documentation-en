# FreezeBalanceV2

Freeze TRX to obtain bandwidth / energy / TronPower (Stake 2.0). No fixed freeze period.

- Service: `Wallet` only

```protobuf
rpc FreezeBalanceV2 (FreezeBalanceV2Contract) returns (TransactionExtention) {}
```

See the corresponding HTTP endpoint at [/wallet/freezebalancev2](../../http/stake-v2/freezebalancev2.md).
