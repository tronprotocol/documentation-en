# BroadcastTransaction

Broadcast a signed transaction.

- Service: `Wallet` only

```protobuf
rpc BroadcastTransaction (Transaction) returns (Return) {}
```

See the corresponding HTTP endpoint at [/wallet/broadcasttransaction](../../http/tx-build-and-broadcast/broadcasttransaction.md).
